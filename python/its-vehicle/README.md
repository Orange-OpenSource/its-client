its-vehicle
===========

The _its-vehicle_ application generates ITS CAM messages, and listens
for any ITS message in its vicinity. It feeds traces of such exchanges
back to an OpenTelemetry collector, to compute round-trip times of
messages in the ITS infra.

Configuration
-------------

See the `its-vehicle.cfg` file for an example configuration file.
